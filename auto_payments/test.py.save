import conexao as conexao

conn = conexao.connect()
cursor = conn.cursor()

print("Teste")

cursor.execute("select * from notfoundpayment;")
result = cursor.fetchall()
for row in result:
    id = int(row[0])
    return i
d
   
